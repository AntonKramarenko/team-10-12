{
  "version": 2,
  "builds": [
    { "src": "./index.js", "use": "@vercel/node" }
  ],
  "routes": [
    {
      "src": "/entities/(.*)",
      "dest": "${VERCEL_URL}/$1"
    }
  ],
  "base": "/entities"
}
